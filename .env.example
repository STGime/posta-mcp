# Required: your Posta API token (starts with "posta_").
# Generate one in the Posta dashboard → Settings → API tokens.
POSTA_API_TOKEN=posta_your_token_here

# Optional: override the API base URL (defaults to production).
# POSTA_BASE_URL=https://api.getposta.app/v1
